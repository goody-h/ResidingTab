<?xml version="1.0" encoding="utf-8"?>

<!-- The parent of the reside layout should be a CoordinatorLayout in order to draw under system decorations-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/reside_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:fitsSystemWindows="false">

    <!-- This is the container view for the residing content. This view is drawn under system decorations -->
    <FrameLayout
        android:id="@+id/reside_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorAccent">

        <ImageView
            android:id="@+id/imageView2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            app:srcCompat="@mipmap/back" />
    </FrameLayout>

    <!-- A frame layout that fits system windows, wrapped around the main layout-->
    <FrameLayout
        android:id="@+id/content_wrapper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <!-- The main layout is added using an include tag. It is drawn over other views-->
        <include
            android:id="@+id/main_content"
            layout="@layout/content_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

</FrameLayout>
